// 'use strict';

// Les différents opérateurs en JavaScript
var add = 1;

// Additions
add += 1;
// Egale à var resultat = add + 1;
console.log(add);

// Soustraction
var sous = 10;
sous -= 5;
console.log(sous);

// Multiplication
var multi = 5;
multi *= 2;
console.log(multi);

// Division
var div = 10;
div /=2;
console.log(div);

// Les conditions
if (condition) {

} else {

}
// else if
if (condition) {

} else if (autre_condition) {
	
}
// switch
switch (key) {
	case value:
		// statements_1
		break;
	default:
		// statements_def
		break;
}

// Structure itérative
// while
while (condition) {
	
}
// do while
do {

} while (condition); 
// for
for (var index = 0; index < array.length; index++) {
	array[index];
}
// for of : Permet de parcourir un objet itérable (array, map, set)
for (const iterator of object) {

}

